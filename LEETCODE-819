class Solution:
    def mostCommonWord(self, paragraph: str, banned: List[str]) -> str:
        a=[i for i in re.split(r'\W+',paragraph.lower()) if i]
        b=[i for i in a if i not in banned]
        return max(b,key=b.count)
